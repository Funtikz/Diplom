image: gradle:alpine

default:
  tags:
    - shelllocal

stages:
  - test

integration_test:
  stage: test
  script:
    - ./gradlew clean test
    - ./gradlew allureReport

  artifacts:
    paths:
      - build/reports/allure-report/allureReport/*

